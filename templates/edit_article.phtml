<form method="POST" action="edit_article.php?id=<?=$idArticle;?>">
    <fieldset>
        <legend>Modifier un article</legend>
        <div>
            <label for="title">Titre de l'article</label>
            <input type="text" id="title" name="title" value="<?=$title;?>">
            <?php if (isset($errors['libArt'])): ?>
                <p class="error"><?=htmlspecialchars($errors['libArt']);?></p>
            <?php endif; ?>
        </div>
        <div>
            <label for="abstract">Résumé de l'article</label>
            <textarea id="abstract" name="abstract"><?=$abstract;?></textarea>
        </div>
        <div>
            <label for="content">Contenu de l'article</label>
            <textarea id="content" name="content"><?=$content;?></textarea>
            <?php if (isset($errors['txtArt'])): ?>
                <p class="error"><?=htmlspecialchars($errors['txtArt']);?></p>
            <?php endif; ?>
        </div>
        <div>
            <label for="image">Image</label>
            <input type="text" id="image" name="image" value="<?=htmlspecialchars($image);?>">
        </div>
        <input type="submit" value="Enregistrer">
    </fieldset>
</form>
    